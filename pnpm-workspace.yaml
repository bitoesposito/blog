# `packages` è richiesto dalle versioni di pnpm precedenti alla 10:
# senza di esso l'install in CI aborta con "packages field missing or empty".
packages:
  - '.'
# `allowBuilds` è la sintassi pnpm 11+ per autorizzare i build script.
allowBuilds:
  esbuild: true
  sharp: true
  workerd: true
