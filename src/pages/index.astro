---
import BlogCard from '@/components/BlogCard.astro'
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import { buttonVariants } from '@/components/ui/button'
import { SITE } from '@/consts'
import Layout from '@/layouts/Layout.astro'
import { getAuthorById, getRecentPosts } from '@/lib/data-utils'
import AuthorCard from '@/components/AuthorCard.astro'

const blog = await getRecentPosts(SITE.featuredPostCount)
const author = await getAuthorById('vitoesposito')
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="Home" />

  <section>
    { author && <AuthorCard author={author} /> }

    <div class="prose mt-4">
      <p class="mt-0">
        <strong>Sviluppatore web</strong> e <strong>designer</strong> con una forte passione per l’informatica.
      </p>
      <p class="mt-0 mb-0">
        Esperto nella progettazione e sviluppo di interfacce web per sistemi complessi ed e-commerce, al momento impegnato nella formazione per lo sviluppo di <Link href="/blog/costruzione-di-cosmetico" external>Cosmetico</Link>.
      </p>
    </div>
  </section>


  <section class="flex flex-col gap-y-4">
    <h2 class="text-2xl font-medium">Gli ultimi post</h2>
    <ul class="flex flex-col gap-y-4">
      {
        blog.map((post) => (
          <li>
            <BlogCard entry={post} />
          </li>
        ))
      }
    </ul>
    <div class="flex justify-center">
      <Link
        href="/blog"
        class={buttonVariants({ variant: 'ghost' }) + ' group'}
      >
        Tutti i post<span
          class="ml-1.5 transition-transform group-hover:translate-x-1"
          >&rarr;</span
        >
      </Link>
    </div>
  </section>
</Layout>
